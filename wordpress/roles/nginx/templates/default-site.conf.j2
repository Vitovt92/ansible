server {
  listen 80;
  listen [::]:80;
  server_name {{ server_hostname }};
  root /var/www/html/{{ server_hostname }};
  server_tokens off;

  include /etc/nginx/conf.d/{{ server_hostname }}_include;
}
